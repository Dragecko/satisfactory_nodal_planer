import { NodeTypes } from 'reactflow';
import BlockNode from './BlockNode';

/**
 * Types de nœuds disponibles dans l'application
 */
export const nodeTypes: NodeTypes = {
  block: BlockNode
};

export default nodeTypes;
