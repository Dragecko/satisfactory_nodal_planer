/**
 * Fichier d'index pour les composants UI réutilisables
 * 
 * Ce fichier centralise l'export de tous les composants UI
 * pour faciliter les imports dans les autres composants.
 */

export { default as BlockListItem } from './BlockListItem';
